node_modules
pb_data
.vscode
build
dist
storage
*.db
*.db-shm
*.db-wal

# Ensure these are always included in the build context
!pb_migrations
!pb_hooks
# Note: .git is NOT ignored — needed for the changelog vite plugin at build time
